const styles = {
  searchContainer: {
    display: 'flex',
    flexDirection: 'column',
    gap: '2rem',
    margin: '3rem auto 0',
    justifyContent: 'center',
    alignItems: 'center',
  },

  searchIcon: {
    cursor: 'pointer',
    marginRight: '1rem',
    '&:hover': {
      transform: 'translateY(-1px)',
    },
  },
};

export default styles;
